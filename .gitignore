__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
build/
dist/
.hypothesis/
# Optional local-only maintenance notes. Never commit personal instructions.
CLAUDE.local.md
# ORVP competition data and derived feature caches -- gigabytes, and the
# competition's terms cover redistribution. `benchmarks/orvp/results/` is
# deliberately NOT ignored: the numbers are the deliverable.
data/
# Out-of-fold predictions: several MB, and regenerable by rerunning the
# benchmark. Kept out of the repo; the scores derived from them are in.
benchmarks/orvp/results/*.npz
