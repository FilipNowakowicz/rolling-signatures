"""The streaming study: does the streaming engine actually pay?

Research code, deliberately kept out of the installable `rollsig` library;
it consumes the public API exactly as an outside user would.

Read `benchmarks/streaming/README.md` first -- it records what was measured,
on what hardware, and which of the three questions came back with an
inconvenient answer.
"""
